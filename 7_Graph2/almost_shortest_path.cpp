#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

int n, m, start, dest;

int main() {
	
	scanf("%d %d", &n, &m);
	scanf("%d %d", &start, &dest);

	for (int i=0; i<m; i++) {
		int u, v, p;
		scanf("%d %d %d", &u, &v, &p);
	}

	return 0;
}
